import { VenueAvatarDirective } from './venue-avatar.directive';

describe('VenueAvatarDirective', () => {
  it('should create an instance', () => {
    const directive = new VenueAvatarDirective();
    expect(directive).toBeTruthy();
  });
});
