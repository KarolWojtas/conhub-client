import { PageContainerDirective } from './page-container.directive';

describe('PageContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new PageContainerDirective();
    expect(directive).toBeTruthy();
  });
});
